{{ form_start(form) }}
<table>
<tr>
<td> {{ form_label(form.nom) }}{{ form_widget(form.nom) }}</td><td>Adresse &nbsp;{{ form_widget(form.adresse) }}</td>
</tr>
<tr>
<td>{{ form_rest(form) }}</td>
</tr>
</table>

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
